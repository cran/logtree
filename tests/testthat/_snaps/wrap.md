# a wrapped tree renders end to end (ascii)

    Code
      wrap_fixture()
    Output
      > Pipeline
      |- > Load config
      |  |- i Reading configuration from the
      |  |    deployment manifest and validating
      |  |    every declared parameter
      |  |- + Done  0.03s
      |- + Done  0.15s

