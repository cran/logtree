% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logger-integration.R
\name{layout_logtree}
\alias{layout_logtree}
\title{A \code{logger} layout that renders through logtree}
\usage{
layout_logtree(
  level,
  msg,
  namespace = NA_character_,
  .logcall = sys.call(),
  .topcall = sys.call(-1),
  .topenv = parent.frame(),
  .timestamp = Sys.time()
)
}
\arguments{
\item{level}{A \code{logger} log level object (e.g. \code{logger::INFO}), as
passed in by \code{logger}'s internal dispatch.}

\item{msg}{Character scalar, already formatted by \code{logger}'s formatter
stage (glue interpolation has already happened by this point).}

\item{namespace, .logcall, .topcall, .topenv, .timestamp}{Unused; accepted
only because \code{logger}'s dispatcher calls every layout with this exact
signature (see \code{logger::layout_simple}).}
}
\value{
\code{character(0)}, invisibly. The record is discarded by
\code{logger::appender_void()} regardless, so its content is irrelevant;
a zero-length character vector matches \code{logger}'s layout contract.
}
\description{
Bridges the \code{logger} package (\url{https://daroczig.github.io/logger/}) into
\code{logtree}'s tree rendering. \code{logger}'s own per-call pipeline is
\code{formatter() -> layout() -> appender()}: only the \emph{layout} stage receives
the structured \code{level} object (an integer with a \code{"level"} attribute
such as \code{"INFO"}) -- \code{appender()} only ever sees a pre-formatted
character line -- so a custom layout, not a custom appender, is the
correct integration point. Register it as \code{logger}'s layout and pair it
with \code{logger::appender_void} (a ready-made no-op) so that \code{logtree}'s
rendering, which happens as a side effect of the layout call, is the
only visible output:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{logger::log_layout(logtree::layout_logtree)
logger::log_appender(logger::appender_void)
}\if{html}{\out{</div>}}

\code{logger} severities map onto \code{logtree} leaf levels as: \code{FATAL}/\code{ERROR} ->
\code{\link[=log_error]{log_error()}}, \code{WARN} -> \code{\link[=log_warn]{log_warn()}}, \code{SUCCESS} -> \code{\link[=log_success]{log_success()}},
\code{INFO} -> \code{\link[=log_info]{log_info()}}, \code{DEBUG}/\code{TRACE} -> \code{\link[=log_debug]{log_debug()}} (\code{logger} has
two debug-ish tiers, \code{logtree} has one, so both collapse to the same
leaf). Note \code{logger}'s own \code{log_threshold()} already gates before the
layout is ever invoked; \code{logtree_threshold()} is then an
independent, second gate applied on top of that -- both legitimately
apply at once, this is not a bug.
}
\examples{
if (requireNamespace("logger", quietly = TRUE)) {
  logtree_reset()
  logger::log_layout(layout_logtree, namespace = "logtree_demo")
  logger::log_appender(logger::appender_void, namespace = "logtree_demo")
  log_step("Demo step")
  logger::log_info("hello", namespace = "logtree_demo")
}
}
