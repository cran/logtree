% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sinks.R
\name{logtree_sink_memory_events}
\alias{logtree_sink_memory_events}
\title{Read a memory sink's collected events}
\usage{
logtree_sink_memory_events(id)
}
\arguments{
\item{id}{A memory sink's id, as returned by \code{\link[=logtree_sink_memory]{logtree_sink_memory()}}.}
}
\value{
A data frame with one row per collected event, oldest first, and zero
rows (with the columns above) when nothing has been logged yet.
}
\description{
Returns everything a \code{\link[=logtree_sink_memory]{logtree_sink_memory()}} sink has collected so far, as a
data frame with one row per event and the same columns a \code{"json"} file sink
writes (see \code{\link[=logtree_sink_file]{logtree_sink_file()}}), so the two views of a run agree:
}
\details{
\tabular{ll}{
   Column \tab What it holds \cr
   \code{ts} \tab when the event was emitted (\code{POSIXct}) \cr
   \code{level} \tab event kind: \code{"open"}, \code{"close"}, \code{"group"}, \code{"group_close"}, \code{"leaf"} \cr
   \code{id}, \code{parent_id}, \code{depth} \tab the node's identity and place in the tree \cr
   \code{label} \tab a step's label, a group's name, or a leaf's message \cr
   \code{elapsed} \tab seconds, on close lines only; \code{NA} elsewhere \cr
   \code{status} \tab a leaf's status, \code{"step"}/\code{"group"} on an opening line, or the resolved status on a close \cr
   \code{fn}, \code{file}, \code{line} \tab the call site, when the \code{trace} theme slot recorded one; \code{NA} otherwise \cr
}
}
\examples{
logtree_reset()
h <- logtree_sink_memory()
f <- function() log_info("Reading config.yml")
invisible(f())
logtree_sink_memory_events(h)
logtree_sink_remove(h)
}
\seealso{
\code{\link[=logtree_sink_memory]{logtree_sink_memory()}}
}
