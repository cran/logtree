% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step.R
\name{log_open}
\alias{log_open}
\title{Open a step under manual lifetime control}
\usage{
log_open(
  msg,
  glyph = NULL,
  parent = NULL,
  group = NULL,
  close = FALSE,
  key = NULL
)
}
\arguments{
\item{msg}{Character scalar. The step's label.}

\item{glyph}{Optional character scalar overriding this step's glyph.}

\item{parent}{Optional step handle (an id returned by \code{log_open()}/
\code{\link[=log_step]{log_step()}}) of a currently-open step to nest this one under. Defaults to
the innermost open step. The target must still be open, else an error.}

\item{group}{Optional named length-1 vector \code{c(name = value)}, as in
\code{\link[=log_step]{log_step()}}.}

\item{close}{Logical. When \code{TRUE}, the step is force-closed silently as soon
as its opening line is printed: a header-only marker with no children and
no \code{Done} line. Defaults to \code{FALSE}.}

\item{key}{Optional character scalar giving this step a stable identity for
re-run reconciliation, as in \code{\link[=log_step]{log_step()}}. At top level the label is used
automatically, so re-running the same \code{log_open()} line re-anchors to that
node instead of nesting under the previous run's leftovers. Ignored when
\code{parent} is supplied.}
}
\value{
The step's id, invisibly. Capture it to pass to \code{\link[=log_close]{log_close()}} or as
another step's \code{parent}.
}
\description{
Like \code{\link[=log_step]{log_step()}} but with no automatic close: the step stays open until
you close it yourself with \code{\link[=log_close]{log_close()}}. This is what you want at top
level (a script or the REPL), where there is no enclosing function frame
for \code{log_step()} to hang its close on. You may also attach the step to a
chosen open \code{parent} rather than the innermost open step, letting you build
the tree by hand.
}
\details{
Opening a step at the same depth as an already-open step -- for example by
linking to a shared \code{parent} -- first closes that sibling and its
descendants, since a new sibling means the previous subtree is done.
}
\examples{
logtree_reset()
s1 <- log_open("Step 1")
log_info("a child line")
log_close(s1)
}
\seealso{
\code{\link[=log_close]{log_close()}}, \code{\link[=log_step]{log_step()}}
}
