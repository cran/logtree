% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sinks.R
\name{logtree_sink_memory}
\alias{logtree_sink_memory}
\title{Collect logged events in memory}
\usage{
logtree_sink_memory(max = 1000, threshold = NULL)
}
\arguments{
\item{max}{Maximum number of events to keep. Once the buffer is full the
oldest events are dropped, so what you read back is always the most recent
\code{max}. Default \code{1000}.}

\item{threshold}{Minimum leaf level to collect, as in \code{\link[=logtree_sink]{logtree_sink()}}.
\code{NULL} (the default) follows the global \code{\link[=logtree_threshold]{logtree_threshold()}}; pass
\code{"debug"} to collect everything a run logged regardless of what the console
was set to show.}
}
\value{
The sink's id, invisibly -- pass it to
\code{\link[=logtree_sink_memory_events]{logtree_sink_memory_events()}} to read the buffer, and to
\code{\link[=logtree_sink_remove]{logtree_sink_remove()}} to stop collecting.
}
\description{
Registers a sink that keeps every event in a buffer instead of writing it
anywhere, so a run's logging can be \emph{asserted on} rather than eyeballed.
Read the buffer back with \code{\link[=logtree_sink_memory_events]{logtree_sink_memory_events()}}. This is the
answer to "did my pipeline log what it should have?" -- previously that meant
capturing console output and pattern-matching the rendered tree.
}
\details{
The buffer is dropped when the sink is removed with \code{\link[=logtree_sink_remove]{logtree_sink_remove()}},
and it is capped: a long-running process cannot grow it without bound.
}
\examples{
logtree_reset()
h <- logtree_sink_memory()

f <- function() {
  log_step("Load data")
  log_warn("coerced 3 rows")
}
invisible(f())

events <- logtree_sink_memory_events(h)
events[, c("level", "label", "status")]

logtree_sink_remove(h)
}
\seealso{
\code{\link[=logtree_sink_memory_events]{logtree_sink_memory_events()}}, \code{\link[=logtree_sink]{logtree_sink()}}
}
