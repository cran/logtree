% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sinks.R
\name{logtree_sink_remove}
\alias{logtree_sink_remove}
\title{Remove registered sinks}
\usage{
logtree_sink_remove(id)
}
\arguments{
\item{id}{Character vector of sink ids, as returned by \code{\link[=logtree_sink]{logtree_sink()}},
\code{\link[=logtree_sink_file]{logtree_sink_file()}} or \code{\link[=logtree_sinks]{logtree_sinks()}}.}
}
\value{
The removed sink functions, invisibly: a named list keyed by the ids
actually removed (empty when none matched). Re-registering one with
\code{\link[=logtree_sink]{logtree_sink()}} restores it, under a fresh id and therefore at the end of
the firing order.
}
\description{
Unregisters one or more sinks by id. Ids that are not registered are ignored,
so cleanup code can run unconditionally. The reserved \code{"console"} id can be
removed like any other, which is how a library silences logtree's console
output outright.
}
\details{
Sinks deliberately survive \code{\link[=logtree_reset]{logtree_reset()}}, so this is the only way to take
one off again.
}
\examples{
logtree_reset()
h <- logtree_sink(function(event) invisible(NULL))
logtree_sinks()
logtree_sink_remove(h)
logtree_sinks()
}
\seealso{
\code{\link[=logtree_sink]{logtree_sink()}}, \code{\link[=logtree_sinks]{logtree_sinks()}}
}
