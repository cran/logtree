% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leaves.R
\name{log_debug}
\alias{log_debug}
\title{Log a debug leaf line}
\usage{
log_debug(msg, close = FALSE, summary = NA)
}
\arguments{
\item{msg}{Character scalar.}

\item{close}{Logical. When \code{TRUE}, force-close the enclosing section after
this line: the line is shown as the section's terminal (corner) line and
its \code{Done} line is suppressed. Defaults to \code{FALSE}.}

\item{summary}{Whether to record this line in the \code{\link[=logtree_summary]{logtree_summary()}}
digest. \code{NA} (default) records it only when it is a warning or error;
\code{TRUE} always records it; \code{FALSE} never does.}
}
\value{
\code{NULL}, invisibly.
}
\description{
The most verbose leaf level, for fine-grained diagnostic detail that
would be noisy at the default verbosity. Shown only when verbosity is
\code{"debug"} (see \code{\link[=logtree_threshold]{logtree_threshold()}}). Like \code{\link[=log_info]{log_info()}} and
\code{\link[=log_success]{log_success()}}, it does not elevate the enclosing step's status --
unlike \code{\link[=log_warn]{log_warn()}}/\code{\link[=log_error]{log_error()}}.
}
\examples{
logtree_reset()
logtree_threshold("debug")
log_debug("Cache miss for key user:42")
logtree_threshold("info")
}
