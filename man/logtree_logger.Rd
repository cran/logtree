% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logger-integration.R
\name{logtree_logger}
\alias{logtree_logger}
\title{Route the \code{logger} package through logtree}
\usage{
logtree_logger(namespace = "global", threshold = TRUE)
}
\arguments{
\item{namespace}{\code{logger} namespace to route. Default \code{"global"}, the
namespace a bare \code{logger::log_info("x")} uses.}

\item{threshold}{Open \code{logger}'s threshold to \code{TRACE} for \code{namespace} so
\code{\link[=logtree_threshold]{logtree_threshold()}} is the only gate? Default \code{TRUE}.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Call once near the top of a script to make the \code{logger} package
(\url{https://daroczig.github.io/logger/}) render through \code{logtree}. It
registers \code{\link[=layout_logtree]{layout_logtree()}} as \code{logger}'s layout and
\code{logger::appender_void} as its appender for \code{namespace}, so from then on
every \code{logger::log_info()} / \code{log_warn()} / ... call in that namespace
prints as a \code{logtree} leaf. This is the one-call form of the manual
\code{logger::log_layout()} + \code{logger::log_appender()} pairing.
}
\details{
With \code{threshold = TRUE} (the default) it also opens \code{logger}'s own
threshold to \code{TRACE} for the namespace. \code{logger} gates on its threshold
\emph{before} the layout runs, so without this a \code{logger::log_debug()} would
never reach \code{logtree}; opening it makes \code{\link[=logtree_threshold]{logtree_threshold()}} the single
effective gate.

Bridge only: it does not install error handling. Wrap the run body in
\code{\link[=with_logging]{with_logging()}} as well when you want failed-run elevation and a summary
line. The change is persistent for the session (matching \code{logger}'s own
global configuration style); there is no automatic teardown.
}
\examples{
if (requireNamespace("logger", quietly = TRUE)) {
  logtree_reset()
  logtree_logger(namespace = "logtree_demo")
  log_step("Demo step")
  logger::log_info("hello", namespace = "logtree_demo")
}
}
\seealso{
\code{\link[=layout_logtree]{layout_logtree()}} for the underlying layout, \code{\link[=with_logging]{with_logging()}}
for top-level error handling.
}
