% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step.R
\name{log_step}
\alias{log_step}
\title{Open a logged step}
\usage{
log_step(
  msg,
  glyph = NULL,
  parent = NULL,
  group = NULL,
  close = FALSE,
  key = NULL
)
}
\arguments{
\item{msg}{Character scalar. The step's label.}

\item{glyph}{Optional character scalar overriding this step's glyph.}

\item{parent}{Optional step handle (an id returned by \code{\link[=log_open]{log_open()}}/
\code{log_step()}) of a currently-open step to nest this one under. Defaults to
the innermost open step. The target must still be open, else an error.}

\item{group}{Optional named length-1 vector \code{c(name = value)}. Adjacent
\code{log_step()} calls sharing the same \code{value} are grouped under a single
\verb{< name >} header line. The value is the match key; the name is displayed.}

\item{close}{Logical. When \code{TRUE}, the step is force-closed silently as soon
as its opening line is printed: a header-only marker with no children and
no \code{Done} line (and no automatic close is registered). Defaults to \code{FALSE}.}

\item{key}{Optional character scalar giving this step a stable identity for
re-run reconciliation. At top level (the global env) the label is used
automatically, so re-running the same line re-anchors to that node instead
of nesting under the previous run's leftovers. Pass \code{key} to override the
automatic label key, or to keep two same-label steps that are open at once
distinct. Ignored when \code{parent} is supplied.}
}
\value{
The step's internal id, invisibly.
}
\description{
\code{log_step()} is intended to be called from \emph{inside a function}: it prints an
opening line for \code{msg} and registers an automatic close that fires when the
\emph{calling} function's frame exits -- whether by normal return, early
\code{return()}, or an uncaught error propagating through it. Because the close is
registered in the caller's frame rather than inside \code{log_step()} itself,
nesting depth always stays in sync, even across errors. At top level, where
there is no enclosing function frame to close on, use \code{\link[=log_open]{log_open()}} /
\code{\link[=log_close]{log_close()}} instead.
}
\details{
Opening a step at the same depth as an already-open step retires that earlier
sibling automatically -- its close line is printed with no explicit
\code{\link[=log_close]{log_close()}} call. In the default nested pattern each \code{log_step()} descends
one level deeper, so this same-level retirement applies when you place steps
side by side via an explicit \code{parent}.
}
\examples{
logtree_reset()
f <- function() {
  log_step("Doing work")
}
f()
}
